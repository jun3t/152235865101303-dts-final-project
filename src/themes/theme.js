import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#7F5283',
        },
        secondary: {
          main: '#EFCA08',
        },
      },
});

export default theme;
